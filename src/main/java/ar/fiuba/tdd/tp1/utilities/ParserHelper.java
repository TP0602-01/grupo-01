package ar.fiuba.tdd.tp1.utilities;

/*
 * Parser Helper is used to convert Strings to integer.
 *
 */
public class ParserHelper {

    public static Integer toInteger(String string) {
        return Integer.parseInt(string);
    }
}
